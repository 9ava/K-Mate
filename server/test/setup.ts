// test/setup.ts
// Jest E2E 테스트 설정 파일

// Jest 글로벌 타입 정의를 명시적으로 import
import '@types/jest'

// 테스트 환경 설정
beforeAll(async () => {
  // E2E 테스트 전 공통 설정
})

afterAll(async () => {
  // E2E 테스트 후 정리 작업
})
