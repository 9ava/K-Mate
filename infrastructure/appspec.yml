version: 0.0
os: linux

# 기존 파일을 덮어쓰도록(이전 빌드 잔재로 인한 충돌 방지)
file_exists_behavior: OVERWRITE

files:
  - source: server-dist/
    destination: /var/www/k-mate/server
  - source: client-dist/
    destination: /var/www/k-mate/client
  - source: systemd/kmate.service
    destination: /etc/systemd/system
  - source: nginx/kmate.conf
    destination: /etc/nginx/conf.d

hooks:
  ApplicationStop:
    - location: scripts/stop_server.sh
      timeout: 120
      runas: root

  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 300
      runas: root

  AfterInstall:
    # ✅ SSM에서 환경변수를 읽어 /srv/k-mate/shared/k-mate.env 생성
    - location: scripts/render_env.sh
      timeout: 180
      runas: root

    - location: scripts/after_install.sh
      timeout: 300
      runas: root

  ApplicationStart:
    - location: scripts/start_server.sh
      timeout: 300
      runas: root

  ValidateService:
    - location: scripts/validate_service.sh
      timeout: 300
      runas: root
